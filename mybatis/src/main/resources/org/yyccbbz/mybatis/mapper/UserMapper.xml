<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yyccbbz.mybatis.mapper.UserMapper">
	
	<select id="queryUserById" parameterType="Long" resultType="User">
		SELECT * FROM tb_user WHERE id = #{id}
	</select>
	
	<!-- 
		resultType: 指定结果集映射的java对象类型
	 -->
	<select id="queryAll" resultType="User">
		SELECT * FROM tb_user
	</select>
	
	<insert id="saveUser" parameterType="User">
		INSERT INTO tb_user (
			id,
			user_name,
			password,
			name,
			age,
			sex,
			birthday,
			created,
			updated
		)
		VALUES
			(
				NULL,
				#{userName},
				#{password},
				#{name},
				#{age},
				#{sex},
				#{birthday},
				NOW(),
				NOW()
			);
	</insert>
	
	<update id="updateUser" parameterType="org.yyccbbz.mybatis.pojo.User">
		UPDATE tb_user
		SET 
		 user_name = #{userName},
		 password = #{password},
		 name = #{name},
		 age = #{age},
		 sex = #{sex},
		 birthday = #{birthday},
		 updated = NOW()
		WHERE
			id = #{id}
	</update>
	
	<delete id="deleteUserById" parameterType="Long">
		DELETE FROM tb_user WHERE id = #{id}
	</delete>
	
	<select id="queryUserListByName" parameterType="String" resultType="User">
		SELECT * FROM tb_user WHERE name like '%${value}%';
	</select>
	
	
</mapper>